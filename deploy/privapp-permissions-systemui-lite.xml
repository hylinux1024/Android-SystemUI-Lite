<?xml version="1.0" encoding="utf-8"?>
<!-- Privileged permissions allowlist for SystemUI-Lite (Phase 1, minimal).

     Must be pushed to /system_ext/etc/permissions/ (or
     /system/etc/permissions/) alongside the APK. Without this file the
     framework will refuse to grant the signature/privileged permissions that
     SystemUI needs. The list mirrors the <uses-permission> entries in
     AndroidManifest.xml (only declared permissions need be whitelisted). -->
<permissions>
    <privapp-permissions package="com.android.systemui">
        <permission name="android.permission.RECEIVE_BOOT_COMPLETED" />
        <permission name="android.permission.STATUS_BAR" />
        <permission name="android.permission.STATUS_BAR_SERVICE" />
        <permission name="android.permission.EXPAND_STATUS_BAR" />
        <permission name="android.permission.INTERNAL_SYSTEM_WINDOW" />
        <permission name="android.permission.SYSTEM_ALERT_WINDOW" />
        <permission name="android.permission.WRITE_SECURE_SETTINGS" />
        <permission name="android.permission.WRITE_SETTINGS" />
        <!-- Wallpaper milestone: these two are signature/privileged and must be
             allowlisted or SystemServer refuses to boot (b/266478665). -->
        <permission name="android.permission.SET_WALLPAPER" />
        <permission name="android.permission.SET_WALLPAPER_DIM_AMOUNT" />
        <permission name="android.permission.READ_WALLPAPER_INTERNAL" />
    </privapp-permissions>
</permissions>
