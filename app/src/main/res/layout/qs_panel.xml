<?xml version="1.0" encoding="utf-8"?>
<!-- Expanded QS panel — matches AOSP portrait expanded QS layout -->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/quick_settings_container"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:clipToPadding="false"
    android:clipChildren="false"
    android:paddingTop="12dp"
    android:paddingBottom="8dp"
    android:layout_marginHorizontal="@dimen/qs_panel_margin_horizontal"
    android:layout_marginTop="8dp">

    <!-- Brightness slider -->
    <LinearLayout
        android:id="@+id/brightness_container"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center_vertical"
        android:paddingHorizontal="16dp"
        android:layout_marginBottom="16dp">

        <ImageView
            android:layout_width="20dp"
            android:layout_height="20dp"
            android:src="@drawable/ic_brightness_low"
            android:layout_marginEnd="12dp"
            android:alpha="0.8" />

        <SeekBar
            android:id="@+id/brightness_slider"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:max="255"
            android:progressTint="#FFFFFFFF"
            android:thumbTint="#FFFFFFFF" />

        <ImageView
            android:layout_width="20dp"
            android:layout_height="20dp"
            android:src="@drawable/ic_brightness_full"
            android:layout_marginStart="12dp"
            android:alpha="0.8" />
    </LinearLayout>

    <!-- QS tile grid — 2 columns (portrait, matches AOSP config) -->
    <GridLayout
        android:id="@+id/qs_tile_grid"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:columnCount="2"
        android:rowCount="4"
        android:useDefaultMargins="false"
        android:paddingHorizontal="4dp" />

</LinearLayout>
